<h1>Friends</h1>
<ul>
  <% unless current_user.friends.blank? %>
    <% current_user.friends.each do |friend| %>
      <li><%= friend.name %></li>
    <% end %>
  <% else %>
    <li>No friends.</li>
  <% end %>
</ul>
